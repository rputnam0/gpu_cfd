# Operations Docs

This folder holds runtime and operator-facing documents for repository automation.

- `symphony_runbook.md` describes the supported Symphony + Linear operating model for `gpu_cfd`.

These docs do not change technical scope. The authority bundle, specs, backlog, and task briefs
remain the implementation source of truth.
