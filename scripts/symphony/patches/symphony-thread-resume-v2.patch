diff --git a/elixir/lib/symphony_elixir/agent_runner.ex b/elixir/lib/symphony_elixir/agent_runner.ex
index 35ea8a0..7136f71 100644
--- a/elixir/lib/symphony_elixir/agent_runner.ex
+++ b/elixir/lib/symphony_elixir/agent_runner.ex
@@ -95,8 +95,14 @@ defmodule SymphonyElixir.AgentRunner do
   defp run_codex_turns(workspace, issue, codex_update_recipient, opts, worker_host) do
     max_turns = Keyword.get(opts, :max_turns, Config.settings!().agent.max_turns)
     issue_state_fetcher = Keyword.get(opts, :issue_state_fetcher, &Tracker.fetch_issue_states_by_ids/1)
+    resume_thread_id = Keyword.get(opts, :resume_thread_id)
 
-    with {:ok, session} <- AppServer.start_session(workspace, worker_host: worker_host) do
+    with {:ok, session} <-
+           AppServer.start_session(
+             workspace,
+             worker_host: worker_host,
+             resume_thread_id: resume_thread_id
+           ) do
       try do
         do_run_codex_turns(session, workspace, issue, codex_update_recipient, opts, issue_state_fetcher, 1, max_turns)
       after
diff --git a/elixir/lib/symphony_elixir/codex/app_server.ex b/elixir/lib/symphony_elixir/codex/app_server.ex
index 7da87ce..842d5e0 100644
--- a/elixir/lib/symphony_elixir/codex/app_server.ex
+++ b/elixir/lib/symphony_elixir/codex/app_server.ex
@@ -39,13 +39,15 @@ defmodule SymphonyElixir.Codex.AppServer do
   @spec start_session(Path.t(), keyword()) :: {:ok, session()} | {:error, term()}
   def start_session(workspace, opts \\ []) do
     worker_host = Keyword.get(opts, :worker_host)
+    resume_thread_id = Keyword.get(opts, :resume_thread_id)
 
     with {:ok, expanded_workspace} <- validate_workspace_cwd(workspace, worker_host),
          {:ok, port} <- start_port(expanded_workspace, worker_host) do
       metadata = port_metadata(port, worker_host)
 
       with {:ok, session_policies} <- session_policies(expanded_workspace, worker_host),
-           {:ok, thread_id} <- do_start_session(port, expanded_workspace, session_policies) do
+           {:ok, thread_id} <-
+             do_start_session(port, expanded_workspace, session_policies, resume_thread_id) do
         {:ok,
          %{
            port: port,
@@ -270,13 +272,28 @@ defmodule SymphonyElixir.Codex.AppServer do
     Config.codex_runtime_settings(workspace, remote: true)
   end
 
-  defp do_start_session(port, workspace, session_policies) do
+  defp do_start_session(port, workspace, session_policies, resume_thread_id) do
     case send_initialize(port) do
-      :ok -> start_thread(port, workspace, session_policies)
+      :ok -> start_or_resume_thread(port, workspace, session_policies, resume_thread_id)
       {:error, reason} -> {:error, reason}
     end
   end
 
+  defp start_or_resume_thread(port, workspace, session_policies, resume_thread_id)
+       when is_binary(resume_thread_id) do
+    trimmed_thread_id = String.trim(resume_thread_id)
+
+    if trimmed_thread_id == "" do
+      start_thread(port, workspace, session_policies)
+    else
+      resume_thread(port, workspace, session_policies, trimmed_thread_id)
+    end
+  end
+
+  defp start_or_resume_thread(port, workspace, session_policies, _resume_thread_id) do
+    start_thread(port, workspace, session_policies)
+  end
+
   defp start_thread(port, workspace, %{approval_policy: approval_policy, thread_sandbox: thread_sandbox}) do
     send_message(port, %{
       "method" => "thread/start",
@@ -301,6 +318,35 @@ defmodule SymphonyElixir.Codex.AppServer do
     end
   end
 
+  defp resume_thread(
+         port,
+         workspace,
+         %{approval_policy: approval_policy, thread_sandbox: thread_sandbox},
+         thread_id
+       ) do
+    send_message(port, %{
+      "method" => "thread/resume",
+      "id" => @thread_start_id,
+      "params" => %{
+        "threadId" => thread_id,
+        "approvalPolicy" => approval_policy,
+        "sandbox" => thread_sandbox,
+        "cwd" => workspace
+      }
+    })
+
+    case await_response(port, @thread_start_id) do
+      {:ok, %{"thread" => thread_payload}} ->
+        case thread_payload do
+          %{"id" => resumed_thread_id} -> {:ok, resumed_thread_id}
+          _ -> {:error, {:invalid_thread_payload, thread_payload}}
+        end
+
+      other ->
+        other
+    end
+  end
+
   defp start_turn(port, thread_id, prompt, issue, workspace, approval_policy, turn_sandbox_policy) do
     send_message(port, %{
       "method" => "turn/start",
diff --git a/elixir/lib/symphony_elixir/orchestrator.ex b/elixir/lib/symphony_elixir/orchestrator.ex
index 3cd8148..8d6cf21 100644
--- a/elixir/lib/symphony_elixir/orchestrator.ex
+++ b/elixir/lib/symphony_elixir/orchestrator.ex
@@ -7,7 +7,7 @@ defmodule SymphonyElixir.Orchestrator do
   require Logger
   import Bitwise, only: [<<<: 2]
 
-  alias SymphonyElixir.{AgentRunner, Config, StatusDashboard, Tracker, Workspace}
+  alias SymphonyElixir.{AgentRunner, Config, ContinuationStore, StatusDashboard, Tracker, Workspace}
   alias SymphonyElixir.Linear.Issue
 
   @continuation_retry_delay_ms 1_000
@@ -34,6 +34,7 @@ defmodule SymphonyElixir.Orchestrator do
       :tick_timer_ref,
       :tick_token,
       running: %{},
+      continuations: %{},
       completed: MapSet.new(),
       claimed: MapSet.new(),
       retry_attempts: %{},
@@ -60,6 +61,7 @@ defmodule SymphonyElixir.Orchestrator do
       poll_check_in_progress: false,
       tick_timer_ref: nil,
       tick_token: nil,
+      continuations: ContinuationStore.load(),
       codex_totals: @empty_codex_totals,
       codex_rate_limits: nil
     }
@@ -127,6 +129,7 @@ defmodule SymphonyElixir.Orchestrator do
       issue_id ->
         {running_entry, state} = pop_running_entry(state, issue_id)
         state = record_session_completion_totals(state, running_entry)
+        state = persist_running_continuation(state, issue_id, running_entry)
         session_id = running_entry_session_id(running_entry)
 
         state =
@@ -140,7 +143,8 @@ defmodule SymphonyElixir.Orchestrator do
                 identifier: running_entry.identifier,
                 delay_type: :continuation,
                 worker_host: Map.get(running_entry, :worker_host),
-                workspace_path: Map.get(running_entry, :workspace_path)
+                workspace_path: Map.get(running_entry, :workspace_path),
+                thread_id: Map.get(running_entry, :thread_id)
               })
 
             _ ->
@@ -152,7 +156,8 @@ defmodule SymphonyElixir.Orchestrator do
                 identifier: running_entry.identifier,
                 error: "agent exited: #{inspect(reason)}",
                 worker_host: Map.get(running_entry, :worker_host),
-                workspace_path: Map.get(running_entry, :workspace_path)
+                workspace_path: Map.get(running_entry, :workspace_path),
+                thread_id: Map.get(running_entry, :thread_id)
               })
           end
 
@@ -176,7 +181,13 @@ defmodule SymphonyElixir.Orchestrator do
           |> maybe_put_runtime_value(:workspace_path, runtime_info[:workspace_path])
 
         notify_dashboard()
-        {:noreply, %{state | running: Map.put(running, issue_id, updated_running_entry)}}
+
+        updated_state =
+          state
+          |> Map.put(:running, Map.put(running, issue_id, updated_running_entry))
+          |> put_continuation(issue_id, continuation_from_running_entry(updated_running_entry))
+
+        {:noreply, updated_state}
     end
   end
 
@@ -197,7 +208,13 @@ defmodule SymphonyElixir.Orchestrator do
           |> apply_codex_rate_limits(update)
 
         notify_dashboard()
-        {:noreply, %{state | running: Map.put(running, issue_id, updated_running_entry)}}
+
+        updated_state =
+          state
+          |> Map.put(:running, Map.put(running, issue_id, updated_running_entry))
+          |> put_continuation(issue_id, continuation_from_running_entry(updated_running_entry))
+
+        {:noreply, updated_state}
     end
   end
 
@@ -405,7 +422,11 @@ defmodule SymphonyElixir.Orchestrator do
   defp refresh_running_issue_state(%State{} = state, %Issue{} = issue) do
     case Map.get(state.running, issue.id) do
       %{issue: _} = running_entry ->
-        %{state | running: Map.put(state.running, issue.id, %{running_entry | issue: issue})}
+        updated_running_entry = %{running_entry | issue: issue}
+
+        state
+        |> Map.put(:running, Map.put(state.running, issue.id, updated_running_entry))
+        |> put_continuation(issue.id, continuation_from_running_entry(updated_running_entry))
 
       _ ->
         state
@@ -418,7 +439,11 @@ defmodule SymphonyElixir.Orchestrator do
         release_issue_claim(state, issue_id)
 
       %{pid: pid, ref: ref, identifier: identifier} = running_entry ->
-        state = record_session_completion_totals(state, running_entry)
+        state =
+          state
+          |> record_session_completion_totals(running_entry)
+          |> persist_running_continuation(issue_id, running_entry)
+
         worker_host = Map.get(running_entry, :worker_host)
 
         if cleanup_workspace do
@@ -439,6 +464,7 @@ defmodule SymphonyElixir.Orchestrator do
             claimed: MapSet.delete(state.claimed, issue_id),
             retry_attempts: Map.delete(state.retry_attempts, issue_id)
         }
+        |> maybe_delete_continuation(issue_id, cleanup_workspace)
 
       _ ->
         release_issue_claim(state, issue_id)
@@ -479,7 +505,10 @@ defmodule SymphonyElixir.Orchestrator do
       |> terminate_running_issue(issue_id, false)
       |> schedule_issue_retry(issue_id, next_attempt, %{
         identifier: identifier,
-        error: "stalled for #{elapsed_ms}ms without codex activity"
+        error: "stalled for #{elapsed_ms}ms without codex activity",
+        worker_host: Map.get(running_entry, :worker_host),
+        workspace_path: Map.get(running_entry, :workspace_path),
+        thread_id: Map.get(running_entry, :thread_id)
       })
     else
       state
@@ -657,10 +686,15 @@ defmodule SymphonyElixir.Orchestrator do
     |> MapSet.new()
   end
 
-  defp dispatch_issue(%State{} = state, issue, attempt \\ nil, preferred_worker_host \\ nil) do
+  defp dispatch_issue(%State{} = state, issue, attempt \\ nil, continuation_override \\ %{}) do
     case revalidate_issue_for_dispatch(issue, &Tracker.fetch_issue_states_by_ids/1, terminal_state_set()) do
       {:ok, %Issue{} = refreshed_issue} ->
-        do_dispatch_issue(state, refreshed_issue, attempt, preferred_worker_host)
+        do_dispatch_issue(
+          state,
+          refreshed_issue,
+          attempt,
+          merged_continuation(state, refreshed_issue.id, continuation_override)
+        )
 
       {:skip, :missing} ->
         Logger.info("Skipping dispatch; issue no longer active or visible: #{issue_context(issue)}")
@@ -677,8 +711,9 @@ defmodule SymphonyElixir.Orchestrator do
     end
   end
 
-  defp do_dispatch_issue(%State{} = state, issue, attempt, preferred_worker_host) do
+  defp do_dispatch_issue(%State{} = state, issue, attempt, continuation) do
     recipient = self()
+    preferred_worker_host = continuation_value(continuation, :worker_host)
 
     case select_worker_host(state, preferred_worker_host) do
       :no_worker_capacity ->
@@ -686,13 +721,22 @@ defmodule SymphonyElixir.Orchestrator do
         state
 
       worker_host ->
-        spawn_issue_on_worker_host(state, issue, attempt, recipient, worker_host)
+        spawn_issue_on_worker_host(state, issue, attempt, recipient, worker_host, continuation)
     end
   end
 
-  defp spawn_issue_on_worker_host(%State{} = state, issue, attempt, recipient, worker_host) do
+  defp spawn_issue_on_worker_host(%State{} = state, issue, attempt, recipient, worker_host, continuation) do
+    resume_thread_id = continuation_value(continuation, :thread_id)
+    workspace_path = continuation_value(continuation, :workspace_path)
+
     case Task.Supervisor.start_child(SymphonyElixir.TaskSupervisor, fn ->
-           AgentRunner.run(issue, recipient, attempt: attempt, worker_host: worker_host)
+           AgentRunner.run(
+             issue,
+             recipient,
+             attempt: attempt,
+             worker_host: worker_host,
+             resume_thread_id: resume_thread_id
+           )
          end) do
       {:ok, pid} ->
         ref = Process.monitor(pid)
@@ -706,7 +750,8 @@ defmodule SymphonyElixir.Orchestrator do
             identifier: issue.identifier,
             issue: issue,
             worker_host: worker_host,
-            workspace_path: nil,
+            workspace_path: workspace_path,
+            thread_id: resume_thread_id,
             session_id: nil,
             last_codex_message: nil,
             last_codex_timestamp: nil,
@@ -737,7 +782,9 @@ defmodule SymphonyElixir.Orchestrator do
         schedule_issue_retry(state, issue.id, next_attempt, %{
           identifier: issue.identifier,
           error: "failed to spawn agent: #{inspect(reason)}",
-          worker_host: worker_host
+          worker_host: worker_host,
+          workspace_path: workspace_path,
+          thread_id: resume_thread_id
         })
     end
   end
@@ -782,6 +829,7 @@ defmodule SymphonyElixir.Orchestrator do
     error = pick_retry_error(previous_retry, metadata)
     worker_host = pick_retry_worker_host(previous_retry, metadata)
     workspace_path = pick_retry_workspace_path(previous_retry, metadata)
+    thread_id = pick_retry_thread_id(previous_retry, metadata)
 
     if is_reference(old_timer) do
       Process.cancel_timer(old_timer)
@@ -804,7 +852,8 @@ defmodule SymphonyElixir.Orchestrator do
             identifier: identifier,
             error: error,
             worker_host: worker_host,
-            workspace_path: workspace_path
+            workspace_path: workspace_path,
+            thread_id: thread_id
           })
     }
   end
@@ -816,7 +865,8 @@ defmodule SymphonyElixir.Orchestrator do
           identifier: Map.get(retry_entry, :identifier),
           error: Map.get(retry_entry, :error),
           worker_host: Map.get(retry_entry, :worker_host),
-          workspace_path: Map.get(retry_entry, :workspace_path)
+          workspace_path: Map.get(retry_entry, :workspace_path),
+          thread_id: Map.get(retry_entry, :thread_id)
         }
 
         {:ok, attempt, metadata, %{state | retry_attempts: Map.delete(state.retry_attempts, issue_id)}}
@@ -854,7 +904,7 @@ defmodule SymphonyElixir.Orchestrator do
         Logger.info("Issue state is terminal: issue_id=#{issue_id} issue_identifier=#{issue.identifier} state=#{issue.state}; removing associated workspace")
 
         cleanup_issue_workspace(issue.identifier, metadata[:worker_host])
-        {:noreply, release_issue_claim(state, issue_id)}
+        {:noreply, state |> release_issue_claim(issue_id) |> maybe_delete_continuation(issue_id, true)}
 
       retry_candidate_issue?(issue, terminal_states) ->
         handle_active_retry(state, issue, attempt, metadata)
@@ -904,7 +954,12 @@ defmodule SymphonyElixir.Orchestrator do
     if retry_candidate_issue?(issue, terminal_state_set()) and
          dispatch_slots_available?(issue, state) and
          worker_slots_available?(state, metadata[:worker_host]) do
-      {:noreply, dispatch_issue(state, issue, attempt, metadata[:worker_host])}
+      {:noreply,
+       dispatch_issue(state, issue, attempt, %{
+         worker_host: metadata[:worker_host],
+         workspace_path: metadata[:workspace_path],
+         thread_id: metadata[:thread_id]
+       })}
     else
       Logger.debug("No available slots for retrying #{issue_context(issue)}; retrying again")
 
@@ -913,10 +968,13 @@ defmodule SymphonyElixir.Orchestrator do
          state,
          issue.id,
          attempt + 1,
-         Map.merge(metadata, %{
+         %{
            identifier: issue.identifier,
-           error: "no available orchestrator slots"
-         })
+           error: "no available orchestrator slots",
+           worker_host: metadata[:worker_host],
+           workspace_path: metadata[:workspace_path],
+           thread_id: metadata[:thread_id]
+         }
        )}
     end
   end
@@ -964,6 +1022,10 @@ defmodule SymphonyElixir.Orchestrator do
     metadata[:workspace_path] || Map.get(previous_retry, :workspace_path)
   end
 
+  defp pick_retry_thread_id(previous_retry, metadata) do
+    metadata[:thread_id] || Map.get(previous_retry, :thread_id)
+  end
+
   defp maybe_put_runtime_value(running_entry, _key, nil), do: running_entry
 
   defp maybe_put_runtime_value(running_entry, key, value) when is_map(running_entry) do
@@ -1112,6 +1174,7 @@ defmodule SymphonyElixir.Orchestrator do
           state: metadata.issue.state,
           worker_host: Map.get(metadata, :worker_host),
           workspace_path: Map.get(metadata, :workspace_path),
+          thread_id: Map.get(metadata, :thread_id),
           session_id: metadata.session_id,
           codex_app_server_pid: metadata.codex_app_server_pid,
           codex_input_tokens: metadata.codex_input_tokens,
@@ -1136,7 +1199,8 @@ defmodule SymphonyElixir.Orchestrator do
           identifier: Map.get(retry, :identifier),
           error: Map.get(retry, :error),
           worker_host: Map.get(retry, :worker_host),
-          workspace_path: Map.get(retry, :workspace_path)
+          workspace_path: Map.get(retry, :workspace_path),
+          thread_id: Map.get(retry, :thread_id)
         }
       end)
 
@@ -1184,6 +1248,7 @@ defmodule SymphonyElixir.Orchestrator do
       Map.merge(running_entry, %{
         last_codex_timestamp: timestamp,
         last_codex_message: summarize_codex_update(update),
+        thread_id: thread_id_for_update(Map.get(running_entry, :thread_id), update),
         session_id: session_id_for_update(running_entry.session_id, update),
         last_codex_event: event,
         codex_app_server_pid: codex_app_server_pid_for_update(codex_app_server_pid, update),
@@ -1217,6 +1282,11 @@ defmodule SymphonyElixir.Orchestrator do
 
   defp session_id_for_update(existing, _update), do: existing
 
+  defp thread_id_for_update(_existing, %{thread_id: thread_id}) when is_binary(thread_id),
+    do: thread_id
+
+  defp thread_id_for_update(existing, _update), do: existing
+
   defp turn_count_for_update(existing_count, existing_session_id, %{
          event: :session_started,
          session_id: session_id
@@ -1636,6 +1706,79 @@ defmodule SymphonyElixir.Orchestrator do
     end
   end
 
+  defp persist_running_continuation(state, issue_id, running_entry) do
+    put_continuation(state, issue_id, continuation_from_running_entry(running_entry))
+  end
+
+  defp continuation_from_running_entry(running_entry) when is_map(running_entry) do
+    %{
+      identifier: Map.get(running_entry, :identifier),
+      worker_host: Map.get(running_entry, :worker_host),
+      workspace_path: Map.get(running_entry, :workspace_path),
+      thread_id: Map.get(running_entry, :thread_id),
+      updated_at: DateTime.utc_now() |> DateTime.truncate(:second) |> DateTime.to_iso8601()
+    }
+    |> Enum.reject(fn {_key, value} -> is_nil(value) end)
+    |> Map.new()
+  end
+
+  defp continuation_from_running_entry(_running_entry), do: %{}
+
+  defp merged_continuation(%State{} = state, issue_id, override) when is_binary(issue_id) do
+    Map.merge(
+      Map.get(state.continuations, issue_id, %{}),
+      normalize_continuation_override(override)
+    )
+  end
+
+  defp merged_continuation(%State{}, _issue_id, override), do: normalize_continuation_override(override)
+
+  defp normalize_continuation_override(override) when is_map(override) do
+    override
+    |> Enum.reject(fn {_key, value} -> is_nil(value) end)
+    |> Map.new()
+  end
+
+  defp normalize_continuation_override(_override), do: %{}
+
+  defp continuation_value(continuation, key) when is_map(continuation) and is_atom(key) do
+    Map.get(continuation, key) || Map.get(continuation, Atom.to_string(key))
+  end
+
+  defp put_continuation(%State{} = state, issue_id, continuation)
+       when is_binary(issue_id) and is_map(continuation) do
+    sanitized =
+      continuation
+      |> Enum.reject(fn {_key, value} -> is_nil(value) end)
+      |> Map.new()
+
+    if sanitized == %{} do
+      state
+    else
+      continuations = Map.put(state.continuations, issue_id, sanitized)
+      persist_continuations(continuations)
+      %{state | continuations: continuations}
+    end
+  end
+
+  defp maybe_delete_continuation(%State{} = state, issue_id, true) when is_binary(issue_id) do
+    continuations = Map.delete(state.continuations, issue_id)
+    persist_continuations(continuations)
+    %{state | continuations: continuations}
+  end
+
+  defp maybe_delete_continuation(%State{} = state, _issue_id, _cleanup_workspace), do: state
+
+  defp persist_continuations(continuations) when is_map(continuations) do
+    case ContinuationStore.persist(continuations) do
+      :ok ->
+        :ok
+
+      {:error, reason} ->
+        Logger.warning("Failed to persist continuation metadata: #{inspect(reason)}")
+    end
+  end
+
   defp running_seconds(%DateTime{} = started_at, %DateTime{} = now) do
     max(0, DateTime.diff(now, started_at, :second))
   end
diff --git a/elixir/lib/symphony_elixir_web/presenter.ex b/elixir/lib/symphony_elixir_web/presenter.ex
index 1063cf7..b28e040 100644
--- a/elixir/lib/symphony_elixir_web/presenter.ex
+++ b/elixir/lib/symphony_elixir_web/presenter.ex
@@ -102,6 +102,7 @@ defmodule SymphonyElixirWeb.Presenter do
       state: entry.state,
       worker_host: Map.get(entry, :worker_host),
       workspace_path: Map.get(entry, :workspace_path),
+      thread_id: Map.get(entry, :thread_id),
       session_id: entry.session_id,
       turn_count: Map.get(entry, :turn_count, 0),
       last_event: entry.last_codex_event,
@@ -124,7 +125,8 @@ defmodule SymphonyElixirWeb.Presenter do
       due_at: due_at_iso8601(entry.due_in_ms),
       error: entry.error,
       worker_host: Map.get(entry, :worker_host),
-      workspace_path: Map.get(entry, :workspace_path)
+      workspace_path: Map.get(entry, :workspace_path),
+      thread_id: Map.get(entry, :thread_id)
     }
   end
 
@@ -132,6 +134,7 @@ defmodule SymphonyElixirWeb.Presenter do
     %{
       worker_host: Map.get(running, :worker_host),
       workspace_path: Map.get(running, :workspace_path),
+      thread_id: Map.get(running, :thread_id),
       session_id: running.session_id,
       turn_count: Map.get(running, :turn_count, 0),
       state: running.state,
@@ -153,7 +156,8 @@ defmodule SymphonyElixirWeb.Presenter do
       due_at: due_at_iso8601(retry.due_in_ms),
       error: retry.error,
       worker_host: Map.get(retry, :worker_host),
-      workspace_path: Map.get(retry, :workspace_path)
+      workspace_path: Map.get(retry, :workspace_path),
+      thread_id: Map.get(retry, :thread_id)
     }
   end
 
diff --git a/elixir/test/symphony_elixir/app_server_test.exs b/elixir/test/symphony_elixir/app_server_test.exs
index b7fab15..09554cf 100644
--- a/elixir/test/symphony_elixir/app_server_test.exs
+++ b/elixir/test/symphony_elixir/app_server_test.exs
@@ -1330,4 +1330,117 @@ defmodule SymphonyElixir.AppServerTest do
       File.rm_rf(test_root)
     end
   end
+
+  test "app server resumes an existing thread when resume_thread_id is provided" do
+    test_root =
+      Path.join(
+        System.tmp_dir!(),
+        "symphony-elixir-app-server-resume-thread-#{System.unique_integer([:positive])}"
+      )
+
+    try do
+      workspace_root = Path.join(test_root, "workspaces")
+      workspace = Path.join(workspace_root, "MT-THREAD")
+      codex_binary = Path.join(test_root, "fake-codex")
+      trace_file = Path.join(test_root, "codex-resume.trace")
+      previous_trace = System.get_env("SYMP_TEST_CODEx_TRACE")
+
+      on_exit(fn ->
+        if is_binary(previous_trace) do
+          System.put_env("SYMP_TEST_CODEx_TRACE", previous_trace)
+        else
+          System.delete_env("SYMP_TEST_CODEx_TRACE")
+        end
+      end)
+
+      System.put_env("SYMP_TEST_CODEx_TRACE", trace_file)
+      File.mkdir_p!(workspace)
+
+      File.write!(codex_binary, """
+      #!/bin/sh
+      trace_file="${SYMP_TEST_CODEx_TRACE:-/tmp/codex-resume.trace}"
+      count=0
+
+      while IFS= read -r line; do
+        count=$((count + 1))
+        printf 'JSON:%s\\n' "$line" >> "$trace_file"
+
+        case "$count" in
+          1)
+            printf '%s\\n' '{"id":1,"result":{}}'
+            ;;
+          2)
+            printf '%s\\n' '{"id":2,"result":{"thread":{"id":"thread-existing"}}}'
+            ;;
+          3)
+            printf '%s\\n' '{"id":3,"result":{"turn":{"id":"turn-resumed"}}}'
+            ;;
+          4)
+            printf '%s\\n' '{"method":"turn/completed"}'
+            exit 0
+            ;;
+          *)
+            exit 0
+            ;;
+        esac
+      done
+      """)
+
+      File.chmod!(codex_binary, 0o755)
+
+      write_workflow_file!(Workflow.workflow_file_path(),
+        workspace_root: workspace_root,
+        codex_command: "#{codex_binary} app-server"
+      )
+
+      issue = %Issue{
+        id: "issue-resume-thread",
+        identifier: "MT-THREAD",
+        title: "Resume an existing thread",
+        description: "Validate thread resume",
+        state: "Rework",
+        url: "https://example.org/issues/MT-THREAD",
+        labels: ["backend"]
+      }
+
+      assert {:ok, result} =
+               AppServer.run(
+                 workspace,
+                 "Resume the existing worker thread",
+                 issue,
+                 resume_thread_id: "thread-existing"
+               )
+
+      assert result.thread_id == "thread-existing"
+
+      lines = File.read!(trace_file) |> String.split("\n", trim: true)
+
+      assert Enum.any?(lines, fn line ->
+               if String.starts_with?(line, "JSON:") do
+                 line
+                 |> String.trim_leading("JSON:")
+                 |> Jason.decode!()
+                 |> then(fn payload ->
+                   payload["method"] == "thread/resume" &&
+                     get_in(payload, ["params", "threadId"]) == "thread-existing"
+                 end)
+               else
+                 false
+               end
+             end)
+
+      refute Enum.any?(lines, fn line ->
+               if String.starts_with?(line, "JSON:") do
+                 line
+                 |> String.trim_leading("JSON:")
+                 |> Jason.decode!()
+                 |> then(fn payload -> payload["method"] == "thread/start" end)
+               else
+                 false
+               end
+             end)
+    after
+      File.rm_rf(test_root)
+    end
+  end
 end
diff --git a/elixir/test/symphony_elixir/core_test.exs b/elixir/test/symphony_elixir/core_test.exs
index 2e33239..db96409 100644
--- a/elixir/test/symphony_elixir/core_test.exs
+++ b/elixir/test/symphony_elixir/core_test.exs
@@ -258,10 +258,14 @@ defmodule SymphonyElixir.CoreTest do
             pid: agent_pid,
             ref: nil,
             identifier: issue_identifier,
+            worker_host: "worker-dormant",
+            workspace_path: workspace,
+            thread_id: "thread-dormant",
             issue: %Issue{id: issue_id, state: "Todo", identifier: issue_identifier},
             started_at: DateTime.utc_now()
           }
         },
+        continuations: %{},
         claimed: MapSet.new([issue_id]),
         codex_totals: %{input_tokens: 0, output_tokens: 0, total_tokens: 0, seconds_running: 0},
         retry_attempts: %{}
@@ -282,6 +286,9 @@ defmodule SymphonyElixir.CoreTest do
       refute MapSet.member?(updated_state.claimed, issue_id)
       refute Process.alive?(agent_pid)
       assert File.exists?(workspace)
+
+      assert %{thread_id: "thread-dormant", worker_host: "worker-dormant"} =
+               updated_state.continuations[issue_id]
     after
       File.rm_rf(test_root)
     end
@@ -321,10 +328,13 @@ defmodule SymphonyElixir.CoreTest do
             pid: agent_pid,
             ref: nil,
             identifier: issue_identifier,
+            workspace_path: workspace,
+            thread_id: "thread-terminal",
             issue: %Issue{id: issue_id, state: "In Progress", identifier: issue_identifier},
             started_at: DateTime.utc_now()
           }
         },
+        continuations: %{issue_id => %{thread_id: "thread-terminal"}},
         claimed: MapSet.new([issue_id]),
         codex_totals: %{input_tokens: 0, output_tokens: 0, total_tokens: 0, seconds_running: 0},
         retry_attempts: %{}
@@ -345,6 +355,7 @@ defmodule SymphonyElixir.CoreTest do
       refute MapSet.member?(updated_state.claimed, issue_id)
       refute Process.alive?(agent_pid)
       refute File.exists?(workspace)
+      refute Map.has_key?(updated_state.continuations, issue_id)
     after
       File.rm_rf(test_root)
     end
@@ -532,6 +543,9 @@ defmodule SymphonyElixir.CoreTest do
       pid: self(),
       ref: ref,
       identifier: "MT-558",
+      worker_host: "worker-01",
+      workspace_path: "/tmp/workspaces/MT-558",
+      thread_id: "thread-558",
       issue: %Issue{id: issue_id, identifier: "MT-558", state: "In Progress"},
       started_at: DateTime.utc_now()
     }
@@ -549,9 +563,13 @@ defmodule SymphonyElixir.CoreTest do
 
     refute Map.has_key?(state.running, issue_id)
     assert MapSet.member?(state.completed, issue_id)
-    assert %{attempt: 1, due_at_ms: due_at_ms} = state.retry_attempts[issue_id]
+
+    assert %{attempt: 1, due_at_ms: due_at_ms, thread_id: "thread-558"} =
+             state.retry_attempts[issue_id]
+
     assert is_integer(due_at_ms)
     assert_due_in_range(due_at_ms, 500, 1_100)
+    assert %{thread_id: "thread-558", worker_host: "worker-01"} = state.continuations[issue_id]
   end
 
   test "abnormal worker exit increments retry attempt progressively" do
@@ -573,6 +591,9 @@ defmodule SymphonyElixir.CoreTest do
       ref: ref,
       identifier: "MT-559",
       retry_attempt: 2,
+      worker_host: "worker-02",
+      workspace_path: "/tmp/workspaces/MT-559",
+      thread_id: "thread-559",
       issue: %Issue{id: issue_id, identifier: "MT-559", state: "In Progress"},
       started_at: DateTime.utc_now()
     }
@@ -588,10 +609,11 @@ defmodule SymphonyElixir.CoreTest do
     Process.sleep(50)
     state = :sys.get_state(pid)
 
-    assert %{attempt: 3, due_at_ms: due_at_ms, identifier: "MT-559", error: "agent exited: :boom"} =
+    assert %{attempt: 3, due_at_ms: due_at_ms, identifier: "MT-559", error: "agent exited: :boom", thread_id: "thread-559"} =
              state.retry_attempts[issue_id]
 
     assert_due_in_range(due_at_ms, 39_500, 40_500)
+    assert %{thread_id: "thread-559", worker_host: "worker-02"} = state.continuations[issue_id]
   end
 
   test "first abnormal worker exit waits before retrying" do
@@ -612,6 +634,9 @@ defmodule SymphonyElixir.CoreTest do
       pid: self(),
       ref: ref,
       identifier: "MT-560",
+      worker_host: "worker-03",
+      workspace_path: "/tmp/workspaces/MT-560",
+      thread_id: "thread-560",
       issue: %Issue{id: issue_id, identifier: "MT-560", state: "In Progress"},
       started_at: DateTime.utc_now()
     }
@@ -627,10 +652,11 @@ defmodule SymphonyElixir.CoreTest do
     Process.sleep(50)
     state = :sys.get_state(pid)
 
-    assert %{attempt: 1, due_at_ms: due_at_ms, identifier: "MT-560", error: "agent exited: :boom"} =
+    assert %{attempt: 1, due_at_ms: due_at_ms, identifier: "MT-560", error: "agent exited: :boom", thread_id: "thread-560"} =
              state.retry_attempts[issue_id]
 
     assert_due_in_range(due_at_ms, 9_000, 10_500)
+    assert %{thread_id: "thread-560", worker_host: "worker-03"} = state.continuations[issue_id]
   end
 
   test "stale retry timer messages do not consume newer retry entries" do
